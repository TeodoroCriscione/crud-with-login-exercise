<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="{{path('app_home')}}">Navbar</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="{{path('app_home')}}">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{path('app_about')}}">About</a>
          </li>
          
          {% if is_granted(('IS_AUTHENTICATED_FULLY')) %}
          
          <li class="nav-item">
            <a class="nav-link" href="{{path('app_logout')}}">Logout</a>
          </li>

          {% else %}

          <li class="nav-item">
            <a class="nav-link" href="{{path('app_register')}}">Register</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{path('app_login')}}">Login</a>
          </li>
            
          {% endif %}

          {% if is_granted('ROLE_ADMIN') %}
            <a class="nav-link border border-success mx-5" href="{{path('app_product_index')}}">Admin Dashboard</a>
            <a class="nav-link border border-success mx-5" href="{{path('app_landing_page')}}">User Landing Page</a>
            {% else %}
              {% if is_granted(('IS_AUTHENTICATED_FULLY')) %}
                <a class="nav-link border border-success mx-5" href="{{path('app_landing_page')}}">User Landing Page</a>
                <a class="nav-link border border-success mx-5" href="#">User Cart</a>
              {% endif %}
          {% endif %}
          
      </div>
    </div>
</nav>